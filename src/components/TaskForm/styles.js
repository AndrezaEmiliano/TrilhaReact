import styled from "styled-components";
import { FactCheck } from "styled-icons/material";

export const Wrapper = styled.div`
  padding: 2rem;
`;
export const Icon = styled(FactCheck)`
  width: 2rem;
`;
